var insult = function(name)
{
  console.log(name + " you doofus");
}

var increment = function(number)
{
  return number + 1;
}

var doubleArray(inarray)
{
  var ret_array = [];
  for(var ix = 0; ix < inarray.length; ix++)
  {
    ret_array[ix] = inarray[ix] * 2;
  }
  return ret_array;
}
